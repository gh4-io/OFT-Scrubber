# Disposable workspace: real samples, extracted internals, screenshots, release exports.
.staging/

# Build output and restore caches (regenerated by dotnet build).
**/[Bb]in/
**/[Oo]bj/
/dist/

# Real emails and templates must never be committed, wherever they are dropped.
# Synthetic fixtures, if ever needed, are generated by tools/test-core.ps1 instead.
*.oft
*.msg
*.eml

# Local assistant instructions are not project source.
CLAUDE.md
.claude/
docs/PROJECT_STATE.md

# Editor state.
.vs/
*.user
